# Sanity.io Configuration
# Get these values from https://www.sanity.io/manage

# Your Sanity project ID (found in sanity.io/manage)
NEXT_PUBLIC_SANITY_PROJECT_ID=your-project-id

# Dataset name (usually "production")
NEXT_PUBLIC_SANITY_DATASET=production

# API version (Sanity API feature version - use 2024-01-01 for latest stable)
NEXT_PUBLIC_SANITY_API_VERSION=2024-01-01

# Use Sanity CDN for faster content delivery (true for production, false for development)
NEXT_PUBLIC_SANITY_USE_CDN=false

# Secret for webhook revalidation (generate a random string)
# Used by the /api/revalidate endpoint to verify webhook requests
SANITY_REVALIDATE_SECRET=your-random-secret-here
