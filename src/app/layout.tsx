// Root layout - minimal wrapper for all routes
// Each route group has its own layout for specific styling

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return children
}
